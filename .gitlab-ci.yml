default:
  image: shreyasramkumar/harmonizing-mri-scans:latest

services:
  - docker:24.0.2-dind

variables:
  DOCKER_TLS_CERTDIR: ""

build-job: 
  tags:
    - "3080"
    - charles
    - docker
  rules:
    - if: $CI_COMMIT_MESSAGE =~ /\[ci]/
      when: on_success #run on success
    - when: manual 
  script:
    # mount volume with local data folder
    - nvidia-smi
    - ls

